[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"
startCommand = "npm run start"
variables = { NIXPACKS_NODE_VERSION = "20" }

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"